% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{expected_value_y_detailed}
\alias{expected_value_y_detailed}
\title{Expected Value of a Transformed Random Variable with Detailed Explanation}
\usage{
expected_value_y_detailed(a, b, cdf, lower_bound, upper_bound, cutoff_point)
}
\arguments{
\item{a}{Coefficient of X in the linear transformation Y = a * X + b.}

\item{b}{Constant term in the linear transformation Y = a * X + b.}

\item{cdf}{Function defining the cumulative distribution function of X.}

\item{lower_bound}{Lower bound of X for the transformation (e.g., 0 for X >= 0).}

\item{upper_bound}{Upper bound of X for the transformation (e.g., 1 for X <= 1).}

\item{cutoff_point}{Point where Y = 0 if X < cutoff_point (e.g., -1 for X < 0).}
}
\value{
A list containing detailed step-by-step explanation of the calculations and results for E\link{Y}.
}
\description{
Computes the expected value E\link{Y} where Y is a linear transformation of X, with specific conditions on the range of X. Returns detailed step-by-step explanation of the results.
}
\details{
This function integrates over the specified range of X to compute E\link{Y} for a linear transformation of X,
conditioned on X being within a specified range.
}
\examples{
# Define the CDF of X: F(x) = (1/2)x + (1/2), for -1 <= x <= 1
cdf_x <- function(x) {
  if (x < -1) return(0)
  if (x > 1) return(1)
  return(0.5 * x + 0.5)
}
expected_value_y_detailed(a = 8, b = 6, cdf = cdf_x, lower_bound = 0, upper_bound = 1, cutoff_point = 0)
}
